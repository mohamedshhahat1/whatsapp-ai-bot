apiVersion: 1

providers:
  - name: whatsapp-ai-bot
    folder: WhatsApp AI Bot
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
