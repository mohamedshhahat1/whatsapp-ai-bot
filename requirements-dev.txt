# Development & CI tooling
-r requirements.txt

ruff>=0.8
black>=24.10
mypy>=1.13
pytest>=8.3
pytest-asyncio>=0.25
pytest-cov>=6.0

# tests/test_secret_wiring.py parses docker-compose.prod.yml. uvicorn[standard]
# already pulls PyYAML in, but a test depending on a transitive dependency
# breaks the day that extra changes. Declare it.
pyyaml>=6.0
